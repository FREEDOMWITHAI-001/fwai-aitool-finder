# GitHub personal access token (recommended)
# Without this, GitHub API is limited to 60 requests/hour
# Create one at: https://github.com/settings/tokens
# Scopes needed: none (public repo access only)
GITHUB_TOKEN=your_github_token_here

# Secret for the manual HTTP trigger endpoint
# Used to prevent unauthorized manual refreshes
REFRESH_SECRET=your_random_secret_here
